class Tarefa {
    constructor(titulo, descricao, status, datadecriacao) {
      titulo = titulo;
      descricao = descricao;
     status = status;
     datadecriacao = datadecriacao
  
  }

  modelar () {
  }
  }

  
  export default Tarefa
  